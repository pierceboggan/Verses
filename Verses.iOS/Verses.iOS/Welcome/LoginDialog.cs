using System;
using MonoTouch.Foundation;
using MonoTouch.UIKit;

namespace Verses.iOS
{
	/*
	// Subclass of Parse's LogInViewController which basically takes care over all the behind-the-scenes action
	public class LoginDialog : ParseLogInViewController
	{
		public LoginDialog ()
		{

		}

		public override void ViewDidLoad ()
		{
			base.ViewDidLoad ();

			/*
			LogInView.BackgroundColor = UIColor.FromPatternImage ();
			LogInView.Logo = new UIImageView (Images.Logo);
			*/

			// Button appearance
			/*
			LogInView.DismissButton.SetImage (image, UIControlState.Normal);
			LogInView.DismissButton.SetImage (image, UIControlState.Highlighted);

			LogInView.FacebookButton.SetImage (null, UIControlState.Normal);
			LogInView.FacebookButton.SetImage (null, UIControlState.Highlighted);
			LogInView.FacebookButton.SetBackgroundImage (image, UIControlState.Normal);
			LogInView.FacebookButton.SetBackgroundImage (image, UIControlState.Highlighted);
			LogInView.FacebookButton.SetTitle ("", UIControlState.Normal);
			LogInView.FacebookButton.SetTitle ("", UIControlState.Highlighted);

			LogInView.TwitterButton.SetImage (null, UIControlState.Normal);
			LogInView.TwitterButton.SetImage (null, UIControlState.Highlighted);
			LogInView.TwitterButton.SetBackgroundImage (image, UIControlState.Normal);
			LogInView.TwitterButton.SetBackgroundImage (image, UIControlState.Highlighted);
			LogInView.TwitterButton.SetTitle ("", UIControlState.Normal);
			LogInView.TwitterButton.SetTitle ("", UIControlState.Highlighted);

			LogInView.SignUpButton.SetBackgroundImage (image, UIControlState.Normal);
			LogInView.SignUpButton.SetBackgroundImage (image, UIControlState.Highlighted);
			LogInView.SignUpButton.SetTitle ("", UIControlState.Normal);
			LogInView.SignUpButton.SetTitle ("", UIControlState.Highlighted);

			// Login field background
			var FieldBackground = new UIImageView ();
			LogInView.InsertSubview (FieldBackground, 1);

			// Remove text shadow
			LogInView.UsernameField.Layer.ShadowOpacity = 0f;
			LogInView.PasswordField.Layer.ShadowOpacity = 0f;
*/
			// Set text field color
			/*
			LogInView.UsernameField.TextColor
			LoginView.PasswordField.TextColor
			*/
		/*}

/*
		public override void ViewDidLayoutSubviews()
		{
			base.ViewDidLayoutSubviews();

			// New frames because sizing has changed!
			/*
			 *     // Set frame for elements
    [self.logInView.dismissButton setFrame:CGRectMake(10, 10, 87.5, 45.5)];
    [self.logInView.logo setFrame:CGRectMake(66.5, 70, 187, 58.5)];
    [self.logInView.facebookButton setFrame:CGRectMake(35, 287, 120, 40)];
    [self.logInView.twitterButton setFrame:CGRectMake(35+130, 287, 120, 40)];
    [self.logInView.signUpButton setFrame:CGRectMake(35, 385, 250, 40)];
    [self.fieldsBackground setFrame:CGRectMake(35, 145, 250, 100)];*/

	/*	}
	}*/
}